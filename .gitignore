# --- Flutter / Dart (generated) ---
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
mobile/.wrangler/
mobile/android/local.properties
mobile/android/.gradle/
mobile/.metadata

# --- IDE ---
.idea/
.vscode/
*.iml

# --- Python (generated) ---
ml/.venv/
__pycache__/
*.pyc
*.pyo

# --- Large datasets (not redistributed via git; metadata like sources.json kept) ---
sample_data/dataset/
sample_data/reviewed/
sample_data/raw_candidates/nuts/
sample_data/quarantine_duplicates/
sample_data/external_samegrelo_test/
# Candidate images in other raw_candidates classes (keep sources.json + csv/txt)
sample_data/raw_candidates/**/*.jpg
sample_data/raw_candidates/**/*.jpeg
sample_data/raw_candidates/**/*.png
sample_data/raw_candidates/**/*.webp

# --- Large training artifacts (regenerated by training; bundled .tflite is enough) ---
*.keras
ml/output/nut_quality/training_history.png

# --- Archives / temp ---
*.zip
*.tmp

# Keep: small ML artifacts under ml/output/nut_quality/ (json, txt, csv, png,
# the .tflite variants, FAILURE_ANALYSIS.md, and the misclassified/ gallery),
# the bundled mobile model, docs, and source.
